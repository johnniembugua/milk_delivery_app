import 'package:flutter/material.dart';

class GoldPoints extends StatelessWidget {
  const GoldPoints({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Gold Points'),
      ),
    );
  }
}
